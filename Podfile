# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PlayoTest' do
  use_frameworks!

  pod 'Alamofire'
  
  target 'PlayoTestTests' do
    pod 'ViewInspector'
  end
end
